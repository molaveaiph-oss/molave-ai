{
  "name": "molave",
  "private": true,
  "version": "1.0.0",
  "description": "Dental practice management system – monorepo",
  "scripts": {
    "dev": "pnpm --filter @molave/types build && pnpm --filter web dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter @molave/types build && pnpm --filter web build",
    "supabase:start": "supabase start",
    "supabase:deploy": "supabase functions deploy"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
